package com.aldrenstudios.selfreign.ui.theme

import androidx.compose.ui.graphics.Color

// --- Calm accent palette (used sparingly for progress, levels, buttons) ---
val Accent = Color(0xFF81C784)       // soft sage green - primary accent
val OceanBlue = Color(0xFF64B5F6)    // muted ocean blue
val Lavender = Color(0xFFB39DDB)     // soft lavender
val AccentDark = Color(0xFF4C8C5A)   // darker sage for containers
val Danger = Color(0xFFE5837B)       // soft, non-alarming red for relapse

// --- Text tones ---
val OnDarkText = Color(0xFFECEFF1)
val OnDarkSubtle = Color(0xFF9AA0A6)

// --- Surfaces / backgrounds ---
val AmoledBackground = Color(0xFF000000) // true AMOLED black
val AmoledSurface = Color(0xFF121212)    // deep charcoal card
val AmoledSurfaceElevated = Color(0xFF1A1A1C) // slightly raised card
val GrayBackground = Color(0xFF121212)
val GraySurface = Color(0xFF1E1E1E)      // slate card
val MidnightBackground = Color(0xFF0B1020)
val MidnightSurface = Color(0xFF151B30)
