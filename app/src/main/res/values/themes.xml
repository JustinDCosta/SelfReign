<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      Base theme. The actual colors are driven by Compose Material 3 at runtime,
      so this XML theme only needs to provide a true-black background and matching
      system bars for a seamless AMOLED launch experience (no white flash on start).
    -->
    <style name="Theme.SelfReign" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
